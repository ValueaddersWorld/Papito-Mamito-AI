# Railway.toml - Railway deployment configuration  
# Each service should override startCommand in Railway dashboard Settings

[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[service]
internalPort = 8000
